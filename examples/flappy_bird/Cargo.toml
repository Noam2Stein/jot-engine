[package]
edition = "2024"
name    = "flappy_bird"
version = "0.1.0"

[dependencies]
jot = { path = "../../jot", features = [
    "asset",
    "ecs",
    "fixed",
    "game",
    "renderer2d",
    "scheme",
] }

rand        = "0.9.2"
rand_chacha = "0.9.0"

[build-dependencies]
jot = { path = "../../jot", features = [ "asset", "gpu" ] }
