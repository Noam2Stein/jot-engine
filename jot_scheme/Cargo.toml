[package]
edition = "2024"
name    = "jot_scheme"
version = "0.1.0"

[dependencies]
jot_scheme_proc_macros = { path = "proc_macros" }

jot_input = { path = "../jot_input" }

bitvec = "1.0.1"
smallvec = "1.15.1"
tinyset = { git = "https://github.com/Noam2Stein/tinyset", features = [
    "winit",
] }
